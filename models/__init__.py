from typing import Dict

from . import base
from . import train_beco
from . import train_base



#def get_model(config: Dict, nets_dict):
#    model = MODELS_REG.get(config.model.type)
#    return model(config=config, nets_dict=nets_dict)